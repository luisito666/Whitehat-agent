# ---- Engagement: auditoria al master de Kubernetes propio ----
# Self-audit autorizado por el propietario (Luis Penagos).
engagement_id: ENG-K8SMASTER-2026-001
client:
  name: "Cluster K8s personal (lab)"
  contact: "Luis Penagos"
authorization:
  reference: "roe-selfaudit-k8smaster-v1"
  valid_from: "2026-09-01"
  valid_until: "2026-12-31"
scope:
  allowed_targets:
    - "192.168.17.52/32"
rules_of_engagement:
  allowed_techniques:
    - "path-traversal-poc"
    - "metasploit-module"
    - "python-snippet"
  prohibited:
    - "persistence"
    - "dos"
    - "data-exfiltration"
    - "lateral-movement"
    - "privilege-escalation"
  max_evidence_items: 10
# Allowlist EXACTA: solo enumeracion/versionado de K8s y ejecucion no destructiva.
msf:
  allowed_modules:
    - "auxiliary/cloud/kubernetes/enum_kubernetes"
    - "auxiliary/scanner/etcd/version"
    - "auxiliary/scanner/etcd/open_key_scanner"
    - "multi/kubernetes/exec"
